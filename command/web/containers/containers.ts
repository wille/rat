const viewContainer = document.getElementById("views-parent");
const mainViewContainer = document.getElementById("first");
const subViewContainer = document.getElementById("second");

const mainViewTabs = document.getElementById("mainview-tabs");
const subViewTabs = document.getElementById("subview-tabs");

const mainViewElement = document.getElementById("view-main");
const subViewElement = document.getElementById("view-sub");

const main = new MainViewContainer();
const sub = new SubViewContainer();


package common

import "time"

const (
	// Version global version number
	Version = "0.0"

	// ScreenStreamWait milliseconds to wait before adding new screenshot to queue
	ScreenStreamWait = time.Millisecond * 100
)
